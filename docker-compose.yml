services:
  android-test-web:
    image: nginx:alpine
    container_name: android-test-web
    ports:
      - "8080:80"
    volumes:
      - ./data:/usr/share/nginx/html/android
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
